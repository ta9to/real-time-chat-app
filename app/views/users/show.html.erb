<h1><%= @user.status %></h1>

<% if @user.avatar.attached? %>
  <%= image_tag url_for(@user.avatar), alt: "アバター" %>
<% else %>
  <%= image_tag "default_avatar.png" %>
<% end %>

<% if current_user == @user %>
  <%= link_to "プロフィールを編集", edit_user_path(@user) %>
<% end %>
